# Example project context

- Template only: replace with repo facts.
- Architecture: service layer owns business rules.
- Convention: API errors use shared response format.
- Constraint: migrations require rollback notes in PR.
